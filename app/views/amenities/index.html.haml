%p#notice= notice

%h1 Create a New Amenity

= render "form"

%h1 Listing amenities

%table{:class => "table"}
  %thead
    %tr
      %th
      %th
      %th
      %th

  %tbody
    - @amenities.each do |amenity|
      %tr
        %td= amenity.name
        %td= link_to 'Edit', edit_amenity_path(amenity)
        %td= link_to 'Delete', amenity, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'Back', admin_only_path
