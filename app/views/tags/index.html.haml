%p#notice= notice

%h1.tamuForeground Create a New Tag
= render 'form'
%br
%h1.tamuForeground Listing tags
.tamuForeground
  %table{:class => "table"}
    %thead
      %tr
        %th
        %th
        %th
    %tbody
      - @tags.each do |tag|
        %tr
          %td= tag.name
          %td= link_to 'Edit', edit_tag_path(tag)
          %td= link_to 'Delete', tag, method: :delete, data: { confirm: 'Are you sure?' }
  %br
.tamu= link_to 'Back', admin_only_path
